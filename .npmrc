# 固定 pnpm store，避免 store 路径随环境解析结果漂移。
# 若不固定，pnpm 会依运行环境在 <盘根>\.pnpm-store 与 <工作区>\.pnpm-store 之间摇摆；
# 一旦与 node_modules/.modules.yaml 记录的位置不一致，pnpm 会要求先清空 node_modules 再重装，
# 在无 TTY 的自动化环境里该步骤会直接中止（ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY）。
store-dir=E:\WorkSpace\family\.pnpm-store\v11
